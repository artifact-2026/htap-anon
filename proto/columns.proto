syntax = "proto2";

package data;

message Table {
    repeated Row rows = 1;
}

message Row {
  repeated Column columns = 1;
}

message Column {
  optional string name = 1;
  optional bytes value = 2;
} 
